package ctxmesh

// Version is stamped from the product tag at release (ADR 0135). Every SDK ships at one
// version, and sdk-contract.sh reads this constant to prove it.
const Version = "0.1.0-beta.3"
