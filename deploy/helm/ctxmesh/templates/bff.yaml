# ============================================================================
# GENERATED by hack/gen-helm-chart.sh from `kustomize build config/default`.
# DO NOT EDIT BY HAND. Change config/ then run `make helm-generate`.
# No-drift is enforced by `make helm-verify` (chart == kustomize).
# ============================================================================
# The Go BFF serves the static UI + /api behind M11 auth. Its SA is least-
# privilege (ADR 0011): no agent-CRD access — every user-facing CRD op runs
# on the CALLER'S token, so the K8s API server enforces the caller's RBAC.
{{- if .Values.ui.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: ctxmesh
    control-plane: bff
  name: ctxmesh-bff
  namespace: {{ .Values.namespace }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: ctxmesh
    control-plane: bff
  name: ctxmesh-bff-role
  namespace: {{ .Values.namespace }}
rules: []
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: ctxmesh
    control-plane: bff
  name: ctxmesh-bff-rolebinding
  namespace: {{ .Values.namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: ctxmesh-bff-role
subjects:
- kind: ServiceAccount
  name: ctxmesh-bff
  namespace: {{ .Values.namespace }}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: ctxmesh
    control-plane: bff
  name: ctxmesh-bff
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: http
    port: 9090
    protocol: TCP
    targetPort: 9090
  - name: metrics
    port: 9092
    protocol: TCP
    targetPort: 9092
  selector:
    app.kubernetes.io/name: ctxmesh
    control-plane: bff
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: ctxmesh
    control-plane: bff
  name: ctxmesh-bff
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.bff.replicas | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: ctxmesh
      control-plane: bff
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: bff
      labels:
        app.kubernetes.io/name: ctxmesh
        control-plane: bff
    spec:
      containers:
      - env:
        - name: CONTAINER_MEMORY_LIMIT_BYTES
          valueFrom:
            resourceFieldRef:
              containerName: bff
              divisor: "1"
              resource: limits.memory
        - name: PROVIDER_CONNECT_ENABLED
          value: {{ .Values.bff.providerConnect.enabled | quote }}
{{- if .Values.bff.runStore.enabled }}
        - name: RUN_WORKER_DISPATCH
          value: "true"
{{- end }}
        - name: RECIPES_OVERLAY_DIR
          value: /etc/ctxmesh/recipes
        - name: STATELAYER_ADDR
          value: {{ .Values.statelayer.externalAddr | default (printf "ctxmesh-statelayer.%s.svc:6379" .Values.namespace) }}
        - name: COST_ROLLUP_ENABLED
          value: {{ .Values.bff.costRollupEnabled | quote }}
        - name: PLATFORM_GENERATION_MODELS
          value: {{ .Values.bff.generation.platformModels | quote }}
        - name: MCP_ENABLED
          value: {{ .Values.bff.mcp.enabled | quote }}
        - name: MCP_REQUIRE_APPROVAL
          value: {{ .Values.bff.mcp.requireApproval | quote }}
        - name: CONSOLE_URL
          value: {{ .Values.bff.consoleURL | default "" | quote }}
        - name: MCP_CREDENTIAL_NAMESPACE
          value: {{ .Values.bff.mcp.credentialNamespace | default .Values.namespace }}
        - name: TOKEN_SERVICE_URL
          value: {{ .Values.controllerManager.oboEgress.tokenServiceURL | default (printf "%s://ctxmesh-token-service.%s.svc:8443" (ternary "https" "http" .Values.tokenService.tls.required) .Values.namespace) }}
        - name: MODEL_GATEWAY_URL
          value: {{ .Values.bff.modelGatewayURL | default (printf "http://ctxmesh-gateway.%s.svc:4000" .Values.namespace) }}
        - name: MANAGED_AGENT_IMAGE
          value: {{ .Values.bff.managedAgentImage | default "" | quote }}
        - name: INGEST_OCR_URL
          value: {{ .Values.bff.ingestOcrURL | default "" | quote }}
        - name: KNOWLEDGE_RERANK_URL
          value: {{ .Values.bff.knowledgeRerankURL | default "" | quote }}
        - name: DISCOVERY_EMBEDDING_ROUTE
          value: {{ .Values.bff.discoveryEmbeddingRoute | default "" | quote }}
        - name: DISCOVERY_RERANK_URL
          value: {{ .Values.bff.discoveryRerankURL | default "" | quote }}
        - name: ASYNC_BACKEND
          value: {{ .Values.bff.asyncBackend | default "" | quote }}
        - name: NATS_URL
          value: {{ .Values.bff.natsURL | default "" | quote }}
        - name: NATS_CREDENTIALS_FILE
          value: {{ .Values.bff.natsCredentialsFile | default "" | quote }}
        - name: MCP_OBO_REQUIRED
          value: {{ .Values.controllerManager.oboEgress.enabled | quote }}
        - name: MCP_GRANT_HMAC_KEY
          value: {{ .Values.bff.mcp.grantHmacKey | default "" | quote }}
        - name: OIDC_ENABLED
          value: {{ .Values.auth.oidc.enabled | quote }}
        - name: OIDC_ISSUER
          value: {{ if .Values.auth.oidc.enabled }}{{ .Values.auth.oidc.issuer | quote }}{{ else }}""{{ end }}
        - name: OIDC_CLIENT_ID
          value: {{ if .Values.auth.oidc.enabled }}{{ .Values.auth.oidc.client.id | quote }}{{ else }}""{{ end }}
        - name: OBJECT_STORE_ADDR
          value: ctxmesh-objectstore.ctxmesh.svc:9000
        - name: OBJECT_STORE_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              key: rootUser
              name: ctxmesh-objectstore-dev
              optional: true
        - name: OBJECT_STORE_SECRET_KEY
          valueFrom:
            secretKeyRef:
              key: rootPassword
              name: ctxmesh-objectstore-dev
              optional: true
        envFrom:
        - secretRef:
            name: ctxmesh-postgres-dsn
            optional: true
        - secretRef:
            name: bff-adapters
            optional: true
        - secretRef:
            name: bff-capability
            optional: true
        image: {{ printf "%s:%s" .Values.bff.image.repository (.Values.bff.image.tag | default .Chart.AppVersion) }}
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /api/health
            port: 9090
          initialDelaySeconds: 10
          periodSeconds: 20
        name: bff
        ports:
        - containerPort: 9090
          name: http
          protocol: TCP
        - containerPort: 9092
          name: metrics
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /api/health
            port: 9090
          initialDelaySeconds: 3
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 1Gi
          requests:
            cpu: 10m
            memory: 192Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts:
        - mountPath: /etc/ctxmesh/recipes
          name: recipe-overlay
          readOnly: true
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: ctxmesh-bff
      terminationGracePeriodSeconds: 10
      volumes:
      - configMap:
          name: ctxmesh-recipes
          optional: true
        name: recipe-overlay
{{- end }}
