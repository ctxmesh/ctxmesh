# ============================================================================
# GENERATED by hack/gen-helm-chart.sh from `kustomize build config/default`.
# DO NOT EDIT BY HAND. Change config/ then run `make helm-generate`.
# No-drift is enforced by `make helm-verify` (chart == kustomize).
# ============================================================================
# The bundled in-cluster Valkey (statelayer) + MinIO (objectstore) are for
# DEV / TRIAL ONLY (PRD §23). Deterministic dev creds, never real secrets.
# Production sets devDataPlane.enabled=false and brings its own data plane.
{{- if .Values.devDataPlane.enabled }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: objectstore
  name: agent-engine-objectstore-dev
  namespace: {{ .Values.namespace }}
stringData:
  rootPassword: agent-engine-dev-secret
  rootUser: agent-engine-dev
type: Opaque
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: objectstore
  name: agent-engine-objectstore
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: s3
    port: 9000
    protocol: TCP
    targetPort: 9000
  - name: console
    port: 9001
    protocol: TCP
    targetPort: 9001
  selector:
    app.kubernetes.io/name: agent-engine
    control-plane: objectstore
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: statelayer
  name: agent-engine-statelayer
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: valkey
    port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app.kubernetes.io/name: agent-engine
    control-plane: statelayer
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: objectstore
  name: agent-engine-objectstore
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-engine
      control-plane: objectstore
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent-engine
        control-plane: objectstore
    spec:
      containers:
      - args:
        - server
        - /data
        - --console-address
        - :9001
        env:
        - name: MINIO_ROOT_USER
          valueFrom:
            secretKeyRef:
              key: rootUser
              name: agent-engine-objectstore-dev
        - name: MINIO_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              key: rootPassword
              name: agent-engine-objectstore-dev
        image: minio/minio:RELEASE.2025-09-07T16-13-09Z@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /minio/health/live
            port: 9000
          periodSeconds: 15
        name: minio
        ports:
        - containerPort: 9000
          name: s3
          protocol: TCP
        - containerPort: 9001
          name: console
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /minio/health/ready
            port: 9000
          periodSeconds: 5
        resources:
          limits:
            memory: 512Mi
          requests:
            cpu: 50m
            memory: 128Mi
        volumeMounts:
        - mountPath: /data
          name: data
      terminationGracePeriodSeconds: 10
      volumes:
      - emptyDir: {}
        name: data
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: statelayer
  name: agent-engine-statelayer
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-engine
      control-plane: statelayer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agent-engine
        control-plane: statelayer
    spec:
      containers:
      - args:
        - --appendonly
        - "no"
        image: valkey/valkey:8.1.3
        livenessProbe:
          failureThreshold: 3
          periodSeconds: 15
          tcpSocket:
            port: 6379
        name: valkey
        ports:
        - containerPort: 6379
          name: valkey
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          periodSeconds: 5
          tcpSocket:
            port: 6379
        resources:
          limits:
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 128Mi
        volumeMounts:
        - mountPath: /data
          name: data
      terminationGracePeriodSeconds: 10
      volumes:
      - emptyDir: {}
        name: data
{{- end }}
