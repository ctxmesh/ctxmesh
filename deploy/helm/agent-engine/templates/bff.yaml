# ============================================================================
# GENERATED by hack/gen-helm-chart.sh from `kustomize build config/default`.
# DO NOT EDIT BY HAND. Change config/ then run `make helm-generate`.
# No-drift is enforced by `make helm-verify` (chart == kustomize).
# ============================================================================
# The Go BFF serves the static UI + /api behind M11 auth. Its SA is least-
# privilege (ADR 0011): no agent-CRD access — every user-facing CRD op runs
# on the CALLER'S token, so the K8s API server enforces the caller's RBAC.
{{- if .Values.ui.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: bff
  name: agent-engine-bff
  namespace: {{ .Values.namespace }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: bff
  name: agent-engine-bff-role
  namespace: {{ .Values.namespace }}
rules: []
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: bff
  name: agent-engine-bff-rolebinding
  namespace: {{ .Values.namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: agent-engine-bff-role
subjects:
- kind: ServiceAccount
  name: agent-engine-bff
  namespace: {{ .Values.namespace }}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: bff
  name: agent-engine-bff
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: http
    port: 9090
    protocol: TCP
    targetPort: 9090
  selector:
    app.kubernetes.io/name: agent-engine
    control-plane: bff
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: agent-engine
    control-plane: bff
  name: agent-engine-bff
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agent-engine
      control-plane: bff
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: bff
      labels:
        app.kubernetes.io/name: agent-engine
        control-plane: bff
    spec:
      containers:
      - env:
        - name: PROVIDER_CONNECT_ENABLED
          value: {{ .Values.bff.providerConnect.enabled | quote }}
        - name: PLATFORM_GENERATION_MODELS
          value: {{ .Values.bff.generation.platformModels | quote }}
        - name: MCP_ENABLED
          value: {{ .Values.bff.mcp.enabled | quote }}
        - name: MCP_REQUIRE_APPROVAL
          value: {{ .Values.bff.mcp.requireApproval | quote }}
        envFrom:
        - secretRef:
            name: bff-adapters
            optional: true
        image: bff:latest
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /api/health
            port: 9090
          initialDelaySeconds: 10
          periodSeconds: 20
        name: bff
        ports:
        - containerPort: 9090
          name: http
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /api/health
            port: 9090
          initialDelaySeconds: 3
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts: []
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: agent-engine-bff
      terminationGracePeriodSeconds: 10
      volumes: []
{{- end }}
