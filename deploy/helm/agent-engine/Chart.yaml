apiVersion: v2
name: agent-engine
description: >-
  agent-engine — the Kubernetes-native control plane for deploying, scaling, and
  observing LLM agents. This chart deploys the control plane (controller-manager,
  CRDs, the LiteLLM gateway, RBAC personas, metrics wiring) and an optional
  bundled dev data plane (Valkey + MinIO) for dev/trial use only (PRD §23).
# The control-plane + CRD templates are GENERATED from `kustomize build
# config/default` by hack/gen-helm-chart.sh — see templates/ banners and the
# `make helm-verify` no-drift check. Do not hand-edit generated templates.
type: application
# Chart version — bump on chart-shape changes (independent of the app image).
version: 0.1.0
# appVersion is the default controller image tag. It MUST equal the tag baked
# into `kustomize build config/default` (controller:latest) so that, with
# default values, `helm template` == `kustomize build config/default`.
appVersion: "latest"
kubeVersion: ">=1.29.0-0"
keywords:
  - agents
  - llm
  - operator
  - kubernetes
home: https://github.com/ctxmesh/agent-engine
sources:
  - https://github.com/ctxmesh/agent-engine
