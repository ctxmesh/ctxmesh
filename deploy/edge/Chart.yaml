apiVersion: v2
name: platform-edge
description: >-
  Gateway API edge for the agent-engine platform (ADR 0038). Host-routes the console, Langfuse, and
  agents through one gateway. Same manifests local→prod; only values differ (baseDomain, TLS, DNS,
  Service type). Requires a Gateway API implementation (Envoy Gateway) already installed.
type: application
version: 0.1.0
appVersion: "0.1.0"
